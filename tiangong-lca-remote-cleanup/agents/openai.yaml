interface:
  display_name: "TianGong LCA Remote Cleanup"
  short_description: "Plan and verify CLI-backed remote dataset cleanup"
  default_prompt: "Use $tiangong-lca-remote-cleanup to plan remote TianGong dataset cleanup, verify delete support, handle permission failures, and republish corrected lifecyclemodel/process/flow artifacts through the CLI."
